import React from 'react';
declare type Props = {
    valid?: boolean;
    warning?: boolean;
    children?: React.ReactNode | ((valid: boolean) => React.ReactNode);
};
export declare const InsightResult: ({ valid, warning, children }: Props) => JSX.Element;
export {};
